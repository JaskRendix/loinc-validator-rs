I found the official NLM LOINC Mapping Validator stuck in an outdated Node.js implementation, slow and difficult to run on large datasets. So I rebuilt it from scratch in Rust — same logic, modern performance.

Original: https://github.com/lhncbc/loinc-mapping-validator  
Rust rewrite: your GitHub link here

Funny thing is, without doing it I wouldn’t have connected the dots — everything ends up being about connecting dots anyway.